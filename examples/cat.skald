var
    input str.
    linebreak str.
rav

main
    linebreak <- "\n".
    loop
        in input.
        if _IO_ERR do
            die.
        fi
        out input.
        out linebreak.
    pool
niam
