var
    input str.
    io_err int.
    linebreak str.
rav

main
    linebreak <- "\n".
    loop
        io_err <- !(in input).
        if io_err do
            die.
        fi
        out input.
        out linebreak.
    pool
niam
