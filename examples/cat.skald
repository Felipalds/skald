var
    input str.
    linebreak str.
    _IO_ERR bool.
rav

main
    linebreak <- "\n".
    loop
        in input.
        if _IO_ERR do
            stop.
        fi
        out input.
        out linebreak.
    pool
niam
